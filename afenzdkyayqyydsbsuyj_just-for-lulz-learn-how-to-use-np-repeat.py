%time import pandas as pd, numpy as np; pd.Series(np.repeat(*zip(*np.rint(pd.read_csv('/kaggle/input/shopee-sentiment-analysis/train.csv', usecols=['rating'])['rating'].value_counts(normalize=True, sort=False)*len(pd.read_csv('/kaggle/input/shopee-sentiment-analysis/test.csv', usecols=['review_id']))).astype(int).to_dict().items())), name='rating').to_csv('submission.csv', index_label='review_id')