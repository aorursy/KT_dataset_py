from numpy import array



x=array([0, 1.5]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([2.3, 0]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x+y
from numpy import array



x=array([1, 0, 0, 0, 1]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([3, 0, 0, 1.3, 1]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x+y
from numpy import array



x=array([1, 1, 2]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([0, 3, 2]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x+y
from numpy import array



x=array([0, 1.5]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([2.3, 0]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x-y
from numpy import array



x=array([1, 0, 0, 0, 1]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([3, 0, 0, 1.3, 1]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x-y
from numpy import array



x=array([1, 1, 2]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([0, 3, 2]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x-y
from numpy import array



x=array([0, 1.5]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([2.3, 0]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x*y
from numpy import array



x=array([1, 0, 0, 0, 1]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([3, 0, 0, 1.3, 1]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x*y
from numpy import array



x=array([1, 1, 2]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([0, 3, 2]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

x*y
from numpy import array

from numpy import dot



x=array([0, 1.5]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([2.3, 0]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

dot(x,y)
from numpy import array

from numpy import dot



x=array([1, 0, 0, 0, 1]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([3, 0, 0, 1.3, 1]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

dot(x,y) #выводим поточечное произведение векторов x и y
from numpy import array

from numpy import dot



x=array([1, 1, 2]).astype(float) #numpy implement different type of arrays, so we use "astype" to convert the array to array of float

y=array([0, 3, 2]).astype(float) #в numpy существуют разные типы массивов, и мы используем метод "astype", чтобы массив состоял из типа float

dot(x,y)
from math import acos

from numpy import dot

from numpy import array



#пишите свой код ниже

#put your code below.
from math import acos

from numpy import dot

from numpy import array



#пишите свой код ниже

#put your code below.
from math import acos

from numpy import dot

from numpy import array



#пишите свой код ниже

#put your code below.
from math import acos

from numpy import dot

from numpy import array



#пишите свой код ниже

#put your code below.
from math import acos

from numpy import dot

from numpy import array



#пишите свой код ниже

#put your code below.