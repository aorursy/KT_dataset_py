import os
# The kernel output used directly

len(os.listdir('../input/dustin-500file-min-example/'))
# The dataset created from the kernel output -- it's capped at 500 files!

len(os.listdir('../input/lets-see-if-500-files-only/'))